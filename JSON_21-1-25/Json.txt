JSON****
--JSON stands for Javascript object Notation
--JSON is the Javascript technique used to transfer the data between client to server and server to client.

SYNTAX:
JS object:-
***************
let a = {
        "name":"Sanyu",
        "age"=19
    }    

JSON object:-
***************
let a = {
        "name":"Sanyu",
        "age"=19
    } 

--For creation of the JSON file we need to create is as "Filename.json" 

JSON Methods
1. JSON.Stringify()-Used to convert the JS object to JSON Object
2. JSON.Parse()-Used to convert JSON object to JS object

Asynchronous in Javascript:-
***************************
=Javascript is by default syncronous ie (single threaded).
=Asynchronousis used to perform multiple tasks at a time

1)setTimeout():- it is the web api used to perform asyn operations with certain time intervals
    SYNTAX:setTimeout(()=>{
         ..................
         )}
    
  
2)setInterval():-Used to perform api operations at certain time intervals
    SYNTAX:setInterval(()=>{
         ..................
         )}


3)promise:-Promise is an object which represents eventual completion or failures of Asynchronous operations.
      *States of Promise
        1)pending State
        2)Fulfilled State
        3)Rejected State
SYNTAX:
    let variable = new Promise((resolve, reject)=>{
                console.log("");
    {);

    To execute the promise we have instance methods:-
    1)then():It is used to execute success or fulfilled state results
    2)catch():It is used to execute rejected state results
    3)finally():THis will get executed irrespective of the executoin ie success or failure.


4)async and await
*async was recently introduced in the Javascript
*async keyword converts synchronouos function into Asynchronous function and starts the promise.

*await keyword make the function to wait until promise completes.




******************
CRUD operations
Create - Insert
Read - Get
Update - Put
Delete - Delete(Destroy)

URL:https://jsonplaceholder.typicode.com/users


*************************
DOM
=DOCUMENT OBJECT MODEL
DOM is a web apii used to manipulate th ehtml content
Dom is used to add, remove , update the content in th e html document.

DOM METHODS
***********

1.document.getElementsById(""):-It is used to select the Html element based on the id attribute value
                                With id sttribute we can select only one element(unique element)
2.document.getElementsByClassName(""):-It is used to select html element based on the "class attribute" values
                                       with class attribute we can select multiple elements.
3.document.getElementsByTagName(""):-it is used to select html elements based on their tag name.
4.document.getElementsByName(""):-It is ised to select html elements based on their name.
5.document.querySelector("#id"):-Used to select single element or id.
6.document.querySelectorAll(".class");It is used to select multiple elements.